# .nojeklly

This folder named `.nojeklly` was created as requested.

If you meant something else (for example the special file `.nojekyll` at the repository root to disable GitHub Pages processing), tell me and I can update or move it.